[package]
name = "game-net"
version = "0.1.0"
edition = "2021"
description = "Multiplayer wire protocol and postcard codec"

[dependencies]
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }

[dev-dependencies]
